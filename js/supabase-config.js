// Fill these in from Supabase → Project Settings → API. The anon key is safe to expose
// client-side (it's restricted by Row Level Security), but never put a service_role key here.
window.SUPABASE_URL = 'https://your-project.supabase.co';
window.SUPABASE_ANON_KEY = 'your-anon-key';
